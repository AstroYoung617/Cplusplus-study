解释下面程序片段是否合法

std::cout << "The sum of" << v1;
		  <<" and " << v2;
		  << " is " << v1 + v2 << std::endl;

如果是合法的，输出什么？不合法原因何在？如何修正？

解答：
	不合法，因为在每一行代码的末尾添加了分号，导致下一行的开头不合法，"<<"符号的左边没有ostream对象。
	应该把每一行末尾的分号去掉。
